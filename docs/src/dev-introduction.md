# Developer's README

This chapter covers installation and setup of the codes to run and reproduce the
analysis. It assumes familiarity with the [git](https://git-scm.com/) version
control system, the use of the command-line (or "terminal" or "shell") to run
various tools, and the [Python](https://www.python.org/) programming language.

{{#include ../../README.md:5:}}
