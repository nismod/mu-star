# To install this environment:
# micromamba create -f environment.yaml -y
name: mu-star
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.12
  - pip:
      - -e .
      - nismod-snail
  - dask
  - geopandas
  - geoviews
  - hatchling
  - hvplot
  - jupyter
  - netCDF4
  - nbstripout
  - notebook
  - numpy
  - matplotlib
  - pyarrow
  - pytest
  - bioconda::snakemake
  - tqdm
  - xarray
  - zarr
