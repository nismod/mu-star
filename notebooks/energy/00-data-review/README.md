# 00 · Data review

Glance at the energy pipeline's inputs and built outputs: product counts, a map
of each built network, the `validation.json` sanity checks, and (optionally) the
VIIRS nightlight raster.

Open `00_data_review.ipynb`.
